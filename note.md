задачи на текущий день:

1. Попробовать поработать с SVG-координатами графика

Задача:

Решения:

!! Не забыть запушить результаты на гитхаб !!

git add .
git commit -m "refactor: применил деструктуризацию объектов и вынес массив транзакций в data"
git push

Графики написаны с нуля на чистом SVG без использования сторонних UI-библиотек

<!-- ======================================= -->

<Activity>
Weekly Activity:
«В процессе работы над BankDash я решил сравнить два подхода к реализации графиков: создание кастомного решения на чистом SVG и использование специализированной библиотеки Recharts. Для этого я реализовал один и тот же график обоими способами в разных ветках (feature/svg-charts и main).Мои выводы:Чистый SVG: Дает полный контроль над разметкой и нулевой вес бандла, но требует написания сложной математики для расчета координат точек и ухудшает читаемость кода. В итоге оставил вариант Recharts. Recharts: Кардинально ускоряет разработку, легко кастомизируется под дизайн-систему макета с помощью React-компонентов и упрощает поддержку кода. В итоге для основного проекта была выбрана Recharts как более коммерчески эффективное и масштабируемое решение». Что нужно до делать: 1. Разобраься как выровнять по центру по оси Y верхнюю серую горизонтальную линию относительно цифр слева. 2. Эти же линии должны быть 100% ширины контента блока
</Activity>

<!-- для ИИ -->

Верстаю макет bankdash для портфолио, на данный момент делаю блок Expense Statistics. Мой стэк: html, scss, js, React/Vite
